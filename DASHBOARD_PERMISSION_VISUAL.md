# Dashboard Permission System - Visual Guide

## 🎯 High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                   DASHBOARD ACCESS PERMISSION                  │
│                                                                 │
│  Current:  Any Logged-In User → Dashboard Access              │
│  New:      Logged-In User + Has 'dashboard.read' → Access     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Permission Flow Diagram

```
User Login
   │
   ├─ Check: Session Valid? → YES
   │
   ├─ NEW: Check: Has 'dashboard.read'?
   │   │
   │   ├─ YES → Load Dashboard
   │   │
   │   └─ NO → Show "Access Denied"
```

---

## 📊 Role-Permission Matrix

```
╔═══════════════════════════════════════════════════════════════════╗
║                   ROLE → PERMISSION MAPPING                      ║
╠════════════════╦════════════════════════════════════════════════╣
║ ROLE           ║ dashboard.read  │ Other Permissions            ║
╠════════════════╬═════════════════╪════════════════════════════════╣
║ Super Admin    ║     ✅ YES      │ users.*, agents.*, ...        ║
║ Admin          ║     ✅ YES      │ users.read, agents.*, ...     ║
║ Manager        ║     ✅ YES      │ agents.read, farmers.*, ...   ║
║ Agent          ║     ❌ NO       │ farmers.*, clusters.read      ║
║ Viewer         ║     ✅ YES      │ *.read (read-only)            ║
╚════════════════╩═════════════════╪════════════════════════════════╝

✅ = Has Access to Dashboard
❌ = No Access to Dashboard
```

---

## 🗂️ Database Relationship Diagram

```
┌──────────────┐         ┌────────────────┐         ┌──────────────┐
│    Users     │         │ User_Roles     │         │    Roles     │
├──────────────┤         ├────────────────┤         ├──────────────┤
│ id           │────1:N──│ userId     (FK)│         │ id           │
│ email        │         │ roleId     (FK)│─────N:1─│ name         │
│ name         │         └────────────────┘         │ permissions []
│ session      │              (Junction Table)       │ - dashboard  │
└──────────────┘                                     │ - users.read │
                                                     │ - agents.upd │
     John                    john → admin            │ - ...        │
     (user)                  (assignment)            │ (permission  │
                                                     │  array)      │
     ↓ Access /dashboard                             └──────────────┘
     ↓ Check: Does John have 'dashboard.read'?
     ↓ Look up: John → admin role
     ↓ Check: admin.permissions includes 'dashboard.read'?
     ↓ YES → Dashboard loads ✅
```

---

## 🔐 Permission Check Code Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ USER VISITS /dashboard                                          │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│ pages/dashboard.js                                              │
│ useEffect() → checkDashboardAccess()                            │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│ Call: hasPermission(userId, 'dashboard.read')                   │
│ ← function from lib/permissions.js                              │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│ lib/permissions.js → getUserPermissions(userId)                 │
│ 1. Find user in database                                        │
│ 2. Get user.userRoles (array of roles)                          │
│ 3. For each role, collect permissions array                     │
│ 4. Combine all permissions into Set                             │
│ 5. Return: ['dashboard.read', 'users.read', ...]               │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────────────┐
│ Check: Is 'dashboard.read' in permissions array?                │
├─────────────────────────────────────────────────────────────────┤
│ IF YES → setHasAccess(true)                                     │
│ IF NO  → setHasAccess(false)                                    │
└──────────────────┬──────────────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
    ✅ YES               ❌ NO
    Load Dashboard      Show Access Denied
    fetchDashboard      Component
    Analytics()         Return Access
                        Denied UI
```

---

## 📝 File Modification Locations

```
lib/permissions.js
├─ Line 20: Add DASHBOARD_READ to PERMISSIONS
│
pages/dashboard.js
├─ Line 1: Add import for PERMISSIONS, hasPermission
├─ Line 60: Add hasAccess state
├─ Line 70: Update useEffect to call checkDashboardAccess
├─ Line 85: Add checkDashboardAccess function
├─ Line 100: Add access denied UI check
│
pages/api/roles/index.js
├─ Line 170: Add 'dashboard.read' to getAllPermissions()
│
scripts/setup-system-roles.js
├─ Lines 25, 40, 55, 65, 80: Add 'dashboard.read' to each role
│
components/PermissionProvider.js
├─ Lines 50-70: Add PERMISSIONS.DASHBOARD_READ to ROLE_PERMISSIONS
```

---

## 🎬 Implementation Timeline

```
TIME 0:00    Read Documentation
             ↓
TIME 2:00    Edit 5 Files (~2 min per file)
             ↓ lib/permissions.js (+1 line)
             ↓ pages/dashboard.js (+50 lines)
             ├─ pages/api/roles/index.js (+1 line)
             ├─ scripts/setup-system-roles.js (+5 lines)
             ├─ components/PermissionProvider.js (+5 lines)
             ↓
TIME 12:00   Restart Dev Server
             $ rm -rf .next && npm run dev
             ↓
TIME 15:00   Test Implementation
             ├─ Test 1: Admin access dashboard ✅
             ├─ Test 2: Agent sees access denied ✅
             ├─ Test 3: Assign permission via UI
             └─ Test 4: Verify access granted ✅
             ↓
TIME 17:00   ✨ DONE - Dashboard Permission Working!
```

---

## 🧪 Test Scenarios Visual

```
┌─────────────────────────────────────────────────────────────────┐
│ TEST SCENARIO 1: Admin Access                                   │
├─────────────────────────────────────────────────────────────────┤
│ User: admin@company.com                                         │
│ Role: Super Admin                                               │
│ Permissions: [dashboard.read, users.*, agents.*, ...]           │
│ Has 'dashboard.read'? ✅ YES                                    │
│ Result: Dashboard loads ✅                                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ TEST SCENARIO 2: Agent Access Denied                            │
├─────────────────────────────────────────────────────────────────┤
│ User: agent@company.com                                         │
│ Role: Agent                                                     │
│ Permissions: [farmers.*, clusters.read]                         │
│ Has 'dashboard.read'? ❌ NO                                     │
│ Result: "Access Denied" message ✅                              │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ TEST SCENARIO 3: Permission Grant                               │
├─────────────────────────────────────────────────────────────────┤
│ User: john@company.com                                          │
│ Initial: No dashboard.read → "Access Denied"                    │
│ Admin Action: Add 'dashboard.read' to John's role               │
│ After Refresh: Has dashboard.read → Dashboard loads ✅          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ TEST SCENARIO 4: Multiple Roles                                 │
├─────────────────────────────────────────────────────────────────┤
│ User: manager@company.com                                       │
│ Role 1: Manager (has: dashboard.read, agents.read)              │
│ Role 2: Reviewer (has: analytics.read, farms.read)              │
│ Combined Permissions: [dashboard.read, agents.read,             │
│                       analytics.read, farms.read]               │
│ Has 'dashboard.read'? ✅ YES (from Role 1)                      │
│ Result: Dashboard loads with combined permissions ✅            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💾 Data Model

```
Before (Current):
┌────────────┐
│ User       │
├────────────┤
│ id         │
│ email      │
│ role       │ ← Single string ("super_admin", "admin", etc.)
│ password   │
│ created    │
└────────────┘
     │
     └─ OLD: Can access dashboard if logged in


After (New):
┌─────────────┐ 1:N ┌──────────────┐ N:1 ┌──────────────┐
│ User        │────→│ UserRoles    │────→│ Roles        │
├─────────────┤     ├──────────────┤     ├──────────────┤
│ id          │     │ userId (FK)  │     │ id           │
│ email       │     │ roleId (FK)  │     │ name         │
│ role        │     └──────────────┘     │ permissions[]│
│ password    │                          └──────────────┘
│ created     │                                 ↓
└─────────────┘                          Example:
                                         permissions: [
     NEW: Can access                       'dashboard.read',
     dashboard if role                     'users.read',
     includes                              'agents.update',
     'dashboard.read'                      ...
                                         ]
```

---

## 🔑 Key Concepts

```
┌────────────────────────────────────────────────────────────────┐
│ 1. PERMISSION                                                  │
│    ↓ Smallest unit of access control                          │
│    ↓ Example: 'dashboard.read'                                │
│    ↓ Stored in roles.permissions array                        │
└────────────────────────────────────────────────────────────────┘
                              ▲
                              │
                         (belongs to)
                              │
┌────────────────────────────────────────────────────────────────┐
│ 2. ROLE                                                        │
│    ↓ Collection of related permissions                        │
│    ↓ Example: "Admin" = [dashboard.read, users.*, ...]        │
│    ↓ Roles can be system (predefined) or custom (admin-created)
└────────────────────────────────────────────────────────────────┘
                              ▲
                              │
                         (assigned to)
                              │
┌────────────────────────────────────────────────────────────────┐
│ 3. USER                                                        │
│    ↓ Person using the system                                  │
│    ↓ Can have multiple roles                                  │
│    ↓ Effective permissions = union of all role permissions    │
└────────────────────────────────────────────────────────────────┘
```

---

## 📚 Permission Hierarchy

```
Application
    │
    ├─ Dashboard
    │   └─ Requires: 'dashboard.read'
    │
    ├─ Users Management
    │   ├─ Create: 'users.create'
    │   ├─ Read: 'users.read'
    │   ├─ Update: 'users.update'
    │   └─ Delete: 'users.delete'
    │
    ├─ Agents Management
    │   ├─ Create: 'agents.create'
    │   ├─ Read: 'agents.read'
    │   ├─ Update: 'agents.update'
    │   └─ Delete: 'agents.delete'
    │
    ├─ Farmers Management
    │   ├─ Create: 'farmers.create'
    │   ├─ Read: 'farmers.read'
    │   ├─ Update: 'farmers.update'
    │   └─ Delete: 'farmers.delete'
    │
    └─ ... (more modules)


NEW: Dashboard now sits at the same level as other
     management features and requires explicit permission
```

---

## ✨ Benefits

```
┌────────────────────────────────────────────────────────────────┐
│ BEFORE: Hard to Control Dashboard Access                       │
├────────────────────────────────────────────────────────────────┤
│ ❌ All logged-in users see dashboard                          │
│ ❌ Can't restrict agents from dashboard                        │
│ ❌ No fine-grained access control                             │
│ ❌ Must change code to modify access                          │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ AFTER: Easy to Control Dashboard Access                        │
├────────────────────────────────────────────────────────────────┤
│ ✅ Dashboard requires 'dashboard.read' permission             │
│ ✅ Can easily restrict agents (no permission)                 │
│ ✅ Fine-grained: Per-role permission control                  │
│ ✅ No code changes needed: Use admin UI to modify             │
│ ✅ Scalable: Works with custom roles                          │
│ ✅ Auditable: Permission assignments tracked                  │
└────────────────────────────────────────────────────────────────┘
```

---

## 🚀 Next Steps

```
1. READ
   └─ Study DASHBOARD_PERMISSION_COMPLETE.md

2. COPY
   └─ Copy code from DASHBOARD_PERMISSION_COPYPASTE.md

3. PASTE
   ├─ lib/permissions.js
   ├─ pages/dashboard.js
   ├─ pages/api/roles/index.js
   ├─ scripts/setup-system-roles.js
   └─ components/PermissionProvider.js

4. RESTART
   └─ rm -rf .next && npm run dev

5. TEST
   ├─ Admin: Can access dashboard
   ├─ Agent: Gets access denied
   ├─ Admin UI: Assign dashboard.read to roles
   └─ Verify: Permissions work as expected

6. DEPLOY
   └─ Push to production
```

---

**Visual Guide Complete** ✨
**Ready to Implement** 🚀
